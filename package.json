{
  "name": "engram-harness",
  "version": "0.2.6",
  "description": "Engram — The AI Harness for Everyone. Personal AI infrastructure that turns any model into your AI.",
  "type": "module",
  "bin": {
    "engram": "dist/cli.js"
  },
  "scripts": {
    "build": "bun build src/cli.ts --outdir dist --target node && bun run build:fix-shebang && bun run build:hooks",
    "build:cli": "bun build src/cli.ts --outdir dist --target node && bun run build:fix-shebang",
    "build:fix-shebang": "node -e \"const f=require('fs');const p='dist/cli.js';let c=f.readFileSync(p,'utf-8');c=c.replace(/^#!.*\\n/,'#!/usr/bin/env node\\n');f.writeFileSync(p,c);f.chmodSync(p,0o755)\"",
    "build:hooks": "bun run scripts/build-hooks.ts",
    "dev": "bun run src/cli.ts",
    "dev:http": "bun run src/cli.ts serve-http",
    "build:compile": "bun build src/cli.ts --compile --outfile engram",
    "typecheck": "tsc --noEmit"
  },
  "exports": {
    ".": "./dist/cli.js",
    "./core": "./src/core/index.ts"
  },
  "files": [
    "dist/cli.js",
    "dist/hooks/",
    "hooks/",
    "skills/",
    "templates/",
    "src/core/",
    "README.md",
    "LICENSE"
  ],
  "dependencies": {
    "@cloudflare/privacypass-ts": "^0.8.1",
    "@inquirer/prompts": "^7.0.0",
    "@modelcontextprotocol/sdk": "^1.26.0",
    "@noble/secp256k1": "^1.7.0",
    "circomlibjs": "^0.1.7",
    "commander": "^13.0.0",
    "ffjavascript": "^0.3.0",
    "snarkjs": "^0.7.5",
    "yaml": "^2.7.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node": "^22.0.0",
    "@types/snarkjs": "^0.7.8",
    "typescript": "^5.7.0"
  },
  "publishConfig": {
    "access": "public"
  },
  "license": "BSL-1.1",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Percival-Labs/engram.git"
  },
  "keywords": [
    "ai",
    "infrastructure",
    "cli",
    "claude-code",
    "personal-ai",
    "engram",
    "ai-assistant"
  ],
  "engines": {
    "node": ">=20.0.0"
  }
}
